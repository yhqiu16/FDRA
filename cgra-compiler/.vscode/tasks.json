{
    "tasks": [
        {
            "type": "shell",
            "label": "make-task",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "dependsOn": "cmake-task"
        },
        {
            "type": "shell",
            "label": "cmake-task",
            "command": "cmake",
            "args": [
                ".."
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ],
    "version": "2.0.0"
}